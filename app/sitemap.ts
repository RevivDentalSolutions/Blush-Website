export const dynamic = "force-static";
import type {MetadataRoute} from "next";import {services} from "@/data/site";export default function sitemap():MetadataRoute.Sitemap{const base="https://blushinkbeautystudio.com";return ["","/about","/services","/gallery","/blog","/faqs","/policies","/contact","/privacy","/terms",...services.map(s=>`/${s.slug}`)].map(url=>({url:base+url,changeFrequency:url===""?"monthly":"yearly"}))}
