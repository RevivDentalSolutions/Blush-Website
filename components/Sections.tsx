import Image from "next/image"; import Link from "next/link"; import {services,site} from "@/data/site";
export function SectionHeading({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="section-heading"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{copy&&<p>{copy}</p>}</div>}
export const Hero=()=> <section className="hero"><div className="hero-media"><Image src={services[0].image!} alt="Editorial beauty portrait representing the Blush brand" fill priority sizes="100vw"/><div className="hero-shade"/></div><div className="hero-content container"><p className="eyebrow">Permanent makeup · North Little Rock, Arkansas</p><h1>Permanent beauty,<br/><em>refined.</em></h1><p className="hero-copy">Natural-looking powder brows, lip blush, permanent eyeliner, lash enhancement, and PMU correction—customized for clients in Little Rock, North Little Rock, Maumelle, and Central Arkansas.</p><div className="button-row"><a className="btn btn-light" href={site.bookingLink}>Book now</a><Link className="text-link light" href="/permanent-makeup">Explore permanent makeup <span>↗</span></Link></div><p className="image-disclosure">Editorial brand imagery · Not a client result</p></div></section>;
export const ServicesGrid=()=> <section className="section services-section container"><SectionHeading eyebrow="Signature services" title="Artistry in every detail." copy="Personalized treatments considered through the lens of balance, restraint, and your individual features."/><div className="service-grid">{services.map((s,i)=><article className={`service-card service-${i%3}`} key={s.slug}>{s.image?<Link href={`/${s.slug}`} className="service-image"><Image src={s.image} alt={s.alt ?? `${s.name} editorial image`} fill sizes="(max-width: 700px) 92vw, 40vw" loading="lazy"/><span className="image-label">Editorial image</span></Link>:<Link href={`/${s.slug}`} className="service-image service-image-neutral" aria-label={`Learn about ${s.name}`}><span>Consultation-led service</span></Link>}<p className="eyebrow">{s.eyebrow}</p><h3><Link href={`/${s.slug}`}>{s.name}</Link></h3><p>{s.desc}</p><Link className="text-link" href={`/${s.slug}`}>Discover the service <span>↗</span></Link></article>)}</div></section>;
export const BookingCTA=()=> <section className="booking-cta booking-cta-neutral"><div className="container booking-content"><p className="eyebrow">Begin with a conversation</p><h2>Your features.<br/><em>Your signature.</em></h2><p>Explore the services, view real client work, and choose the Square appointment option that fits your goals.</p><a className="btn btn-light" href={site.bookingLink}>Book an appointment</a></div></section>;
export const ContactStrip=BookingCTA;
