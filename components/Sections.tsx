import Image from "next/image"; import Link from "next/link"; import {services,site} from "@/data/site";
export function SectionHeading({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="section-heading"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{copy&&<p>{copy}</p>}</div>}
export const Hero=()=> <section className="hero"><div className="hero-media"><Image src={services[0].image} alt="Editorial beauty portrait representing the Blush brand" fill priority sizes="100vw"/><div className="hero-shade"/></div><div className="hero-content container"><p className="eyebrow">North Little Rock, Arkansas</p><h1>Permanent beauty,<br/><em>refined.</em></h1><p className="hero-copy">Customized permanent makeup and advanced skin treatments designed to enhance your natural features with polished, lasting-looking results.</p><div className="button-row"><a className="btn btn-light" href={site.bookingLink}>Book an appointment</a><Link className="text-link light" href="/services">Explore services <span>↗</span></Link></div><p className="image-disclosure">Editorial brand imagery · Not a client result</p></div></section>;
export const ServicesGrid=()=> <section className="section services-section container"><SectionHeading eyebrow="Signature services" title="Artistry in every detail." copy="Personalized treatments considered through the lens of balance, restraint, and your individual features."/><div className="service-grid">{services.map((s,i)=><article className={`service-card service-${i%3}`} key={s.slug}><Link href={`/${s.slug}`} className="service-image"><Image src={s.image} alt={s.alt} fill sizes="(max-width: 700px) 92vw, 40vw" loading="lazy"/><span className="image-label">Editorial image</span></Link><p className="eyebrow">{s.eyebrow}</p><h3><Link href={`/${s.slug}`}>{s.name}</Link></h3><p>{s.desc}</p><Link className="text-link" href={`/${s.slug}`}>Discover the service <span>↗</span></Link></article>)}</div></section>;
export const BookingCTA=()=> <section className="booking-cta"><div className="booking-image"><Image src={services[1].image} alt="Editorial beauty portrait with polished neutral makeup" fill sizes="100vw" loading="lazy"/><div className="hero-shade"/></div><div className="container booking-content"><p className="eyebrow">Begin with a conversation</p><h2>Your features.<br/><em>Your signature.</em></h2><p>Thoughtful artistry begins with listening. Explore your options and plan a treatment that feels distinctly yours.</p><a className="btn btn-light" href={site.bookingLink}>Book an appointment</a></div></section>;
export const ContactStrip=BookingCTA;
